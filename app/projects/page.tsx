import Link from "next/link";
import { Icon } from "@/components/icon";
import { projects } from "@/lib/projects";
import { publishedProjects } from "@/lib/rooted-content";
export const metadata = { title: "Projects & Impact", description: "Explore Rooted In Christ Ministry projects and verified impact." };
export default async function ProjectsPage() { const liveProjects = await publishedProjects(); const visibleProjects = liveProjects.length ? liveProjects : projects; return <main id="main-content" className="simple-page"><section className="page-hero"><div className="container"><p className="eyebrow light">Projects & impact</p><h1>Transparent work. Meaningful outcomes.</h1><p>Project information will be published with care, context, and verified reporting.</p></div></section><section className="container project-list-page"><div className="programme-list-intro"><p className="eyebrow">Our project spaces</p><h2>Service that can be understood and trusted.</h2><p>Projects are shared through verified updates, clear objectives, and responsible reporting. Private beneficiary information is never displayed.</p></div><div className="project-grid">{visibleProjects.map((project) => <article className="project-card" key={project.slug}><div className="project-card-top"><span className="card-kicker">{project.status}</span><span className="project-dot" /></div><h3>{project.title}</h3><p>{project.summary}</p><div className="project-meta"><span>{project.programme}</span><span>{project.location}</span></div><Link href={`/projects/${project.slug}`} aria-label={`View ${project.title} project`}>View project <Icon name="arrow" size={17} /></Link></article>)}</div></section></main>; }
