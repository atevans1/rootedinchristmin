import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { Icon } from "@/components/icon";
import { projects } from "@/lib/projects";
export function generateStaticParams() { return projects.map((project) => ({ slug: project.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const project = projects.find((item) => item.slug === slug); return project ? { title: project.title, description: project.summary } : {}; }
export default async function ProjectDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const project = projects.find((item) => item.slug === slug); if (!project) notFound(); return <main id="main-content" className="simple-page"><section className="page-hero"><div className="container"><p className="eyebrow light">{project.programme}</p><h1>{project.title}</h1><p>{project.summary}</p></div></section><section className="container programme-detail"><div><p className="eyebrow">Project record</p><h2>Building a clear account of work and impact.</h2><p className="detail-copy">Verified objectives, dates, location, beneficiaries reached, images, related stories, and administrator updates will be published here as this project develops.</p><Link href="/projects" className="arrow-link">Back to projects <Icon name="arrow" size={18} /></Link></div><aside className="notice-card"><strong>{project.status}</strong><p>Impact figures and project outcomes are intentionally withheld until they are supplied and approved by authorised ministry administrators.</p></aside></section></main>; }
