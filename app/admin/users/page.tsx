import Link from "next/link";
import { Icon } from "@/components/icon";
import { adminRoles, roleDescriptions } from "@/lib/supabase/roles";
export const metadata = { title: "Admin users", description: "Manage Rooted In Christ Ministry administrator roles." };
export default function AdminUsersPage() { return <main id="main-content" className="admin-shell"><div className="admin-topbar"><div className="container admin-topbar-inner"><Link href="/admin" className="admin-brand"><span className="brand-mark"><Icon name="sprout" size={23} /></span><span><strong>Rooted In Christ</strong><small>Ministry administration</small></span></Link><span className="admin-status">Super Admin controls</span></div></div><div className="container admin-content"><Link href="/admin" className="arrow-link">Back to dashboard <Icon name="arrow" size={18} /></Link><div className="admin-heading admin-users-heading"><div><p className="eyebrow">Access control</p><h1>Assign the right level of trust.</h1><p>Only the Super Admin can manage administrator roles. Client accounts should receive the minimum access needed to run the site.</p></div></div><div className="admin-notice"><strong>Role management is awaiting Supabase connection</strong><p>User invitations and role changes will be enabled after authentication is configured. No account can be changed from this preview.</p></div><section className="admin-section"><div className="section-heading"><div><p className="eyebrow">Approved roles</p><h2>Clear responsibilities.</h2></div></div><div className="role-grid"><article className="role-card role-card-primary"><span>Owner role</span><h3>Super Admin</h3><p>{roleDescriptions.super_admin}</p></article>{adminRoles.map((role) => <article className="role-card" key={role}><span>Client-ready role</span><h3>{role.replaceAll("_", " ")}</h3><p>{roleDescriptions[role]}</p></article>)}</div></section></div></main>; }
