import Link from "next/link";
import { Icon } from "@/components/icon";
const sections = [["Programmes", "Manage programme areas and approved details", "/programmes", "programme_manager"], ["Projects", "Maintain project records and verified updates", "/projects", "programme_manager"], ["Gallery", "Review and publish consented ministry media", "/gallery", "content_manager"], ["Stories", "Draft and publish news, reports, and testimonies", "/stories", "content_manager"], ["Events", "Manage confirmed gatherings and event details", "/events", "content_manager"]];
export const metadata = { title: "Content workspace", description: "Private Rooted In Christ Ministry content management workspace." };
export default function AdminContentPage() { return <main id="main-content" className="admin-shell"><div className="admin-topbar"><div className="container admin-topbar-inner"><Link href="/admin" className="admin-brand"><span className="brand-mark"><Icon name="sprout" size={23} /></span><span><strong>Rooted In Christ</strong><small>Ministry administration</small></span></Link><span className="admin-status">Content workspace</span></div></div><div className="container admin-content"><Link href="/admin" className="arrow-link">Back to dashboard <Icon name="arrow" size={18} /></Link><div className="admin-heading"><div><p className="eyebrow">Content management</p><h1>Keep the public story clear.</h1><p>Manage only verified ministry content. Every section will be protected by its assigned role.</p></div></div><div className="admin-notice"><strong>Publishing connection pending</strong><p>These controls are prepared for Supabase-backed CRUD and approval workflows. No live content is changed from this preview.</p></div><div className="content-section-grid">{sections.map(([title, description, href, role]) => <article className="content-section-card" key={title}><span>{role}</span><h2>{title}</h2><p>{description}</p><Link href={href}>View public section <Icon name="arrow" size={16} /></Link></article>)}</div></div></main>; }
