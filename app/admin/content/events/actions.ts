"use server";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export type EventState = { ok: boolean; message: string };
export async function createEvent(_previous: EventState, formData: FormData): Promise<EventState> { const title = String(formData.get("title") || "").trim(); const category = String(formData.get("category") || "").trim(); const description = String(formData.get("description") || "").trim(); const date = String(formData.get("event_date") || "").trim(); if (title.length < 3 || title.length > 180 || !category || !date || description.length < 10 || description.length > 5000) return { ok: false, message: "Please provide a title, category, date, and description." }; const supabase = await createSupabaseServerClient(); if (!supabase) return { ok: false, message: "The secure content service is being prepared. Please try again later." }; return { ok: false, message: "Event drafting is prepared for the online Supabase connection." }; }
