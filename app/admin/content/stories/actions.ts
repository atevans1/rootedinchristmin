"use server";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export type StoryState = { ok: boolean; message: string };
export async function createStory(_previous: StoryState, formData: FormData): Promise<StoryState> { const title = String(formData.get("title") || "").trim(); const category = String(formData.get("category") || "").trim(); const content = String(formData.get("content") || "").trim(); if (title.length < 3 || title.length > 180 || !category || content.length < 20 || content.length > 10000) return { ok: false, message: "Please provide a title, category, and content between 20 and 10,000 characters." }; const supabase = await createSupabaseServerClient(); if (!supabase) return { ok: false, message: "The secure content service is being prepared. Please try again later." }; const slug = title.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); const { data: { user } } = await supabase.auth.getUser(); const { error } = await supabase.from("posts").insert({ title, slug, category, content, status: "published", published_at: new Date().toISOString(), author_id: user?.id }); return error ? { ok: false, message: "We could not publish this story. Please check the title and try again." } : { ok: true, message: "Story published successfully." }; }
