"use server";
import { createRootedSupabaseServerClient } from "@/lib/supabase/rooted";
export type ProjectState = { ok: boolean; message: string };
export async function createProject(_previous: ProjectState, formData: FormData): Promise<ProjectState> { const title = String(formData.get("title") || "").trim(); const description = String(formData.get("description") || "").trim(); const location = String(formData.get("location") || "").trim(); if (title.length < 3 || title.length > 160 || description.length < 10 || description.length > 5000) return { ok: false, message: "Please provide a title and description between 10 and 5,000 characters." }; const supabase = await createRootedSupabaseServerClient(); if (!supabase) return { ok: false, message: "The secure content service is being prepared. Please try again later." }; const slug = title.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); const { data: { user } } = await supabase.auth.getUser(); const { error } = await supabase.from("projects").insert({ title, slug, description, location, status: "published", created_by: user?.id }); return error ? { ok: false, message: "We could not publish this project. Please check the title and try again." } : { ok: true, message: "Project published successfully." }; }
