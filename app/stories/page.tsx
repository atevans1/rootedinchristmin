import Link from "next/link";
import { Icon } from "@/components/icon";
import { storyCategories } from "@/lib/stories";
export const metadata = { title: "Stories & Updates", description: "Ministry stories, testimonies, and verified updates from Rooted In Christ Ministry." };
export default function StoriesPage() { return <main id="main-content" className="simple-page"><section className="page-hero"><div className="container"><p className="eyebrow light">Stories & updates</p><h1>Hope taking root.</h1><p>Approved ministry news, testimonies, outreach reports, and project updates will be shared here.</p></div></section><section className="container stories-page"><div className="stories-intro"><p className="eyebrow">The journal</p><h2>Words that point to the work.</h2><p>Stories will be published with consent, context, and approval. No testimony or outcome is published automatically.</p></div><div className="story-filter" aria-label="Story categories">{storyCategories.map((category, index) => <span className={index === 0 ? "active" : ""} key={category}>{category}</span>)}</div><div className="stories-empty"><div className="empty-mark"><Icon name="book" size={31} /></div><p className="eyebrow">Stories are being prepared</p><h3>Verified updates will appear here.</h3><p>The ministry dashboard will support drafting, review, approval, scheduling, and publishing for each story category.</p><Link href="/contact" className="arrow-link">Connect with the ministry <Icon name="arrow" size={18} /></Link></div></section></main>; }
