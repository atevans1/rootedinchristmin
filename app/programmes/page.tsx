import Link from "next/link";`r`nimport { publishedRows } from "@/lib/rooted-content";
import { Icon } from "@/components/icon";
import { programmes } from "@/lib/programmes";
export const metadata = { title: "Programmes", description: "Explore Rooted In Christ Ministry programme areas." };
export default async function ProgrammesPage() { return <main id="main-content" className="simple-page"><section className="page-hero"><div className="container"><p className="eyebrow light">Our programmes</p><h1>Practical pathways to lasting impact.</h1><p>Our programme areas connect faith, compassion, and responsible community service.</p></div></section><section className="container programme-list-page"><div className="programme-list-intro"><p className="eyebrow">Programme areas</p><h2>Growing what serves the community.</h2><p>Programme information will be published as authorised ministry administrators verify each area. No beneficiary identities or unverified outcomes are displayed.</p></div><div className="programme-list">{(await publishedRows("programmes")).map((programme) => <article className="programme-list-card" key={programme.slug}><div><p className="card-kicker">{programme.category}</p><h3>{programme.title}</h3><p>{programme.summary}</p></div><div className="programme-list-footer"><span>{programme.status}</span><Link href={`/programmes/${programme.slug}`} aria-label={`View ${programme.title}`}>View programme <Icon name="arrow" size={17} /></Link></div></article>)}</div></section></main>; }
