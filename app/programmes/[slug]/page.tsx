import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { Icon } from "@/components/icon";
import { programmes } from "@/lib/programmes";
export function generateStaticParams() { return programmes.map((programme) => ({ slug: programme.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const programme = programmes.find((item) => item.slug === slug); return programme ? { title: programme.title, description: programme.summary } : {}; }
export default async function ProgrammeDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const programme = programmes.find((item) => item.slug === slug); if (!programme) notFound(); return <main id="main-content" className="simple-page"><section className="page-hero"><div className="container"><p className="eyebrow light">{programme.category}</p><h1>{programme.title}</h1><p>{programme.summary}</p></div></section><section className="container programme-detail"><div><p className="eyebrow">About this programme</p><h2>{programme.description}</h2><p className="detail-copy">Detailed objectives, locations, dates, beneficiaries, gallery items, related stories, and calls to action appears here after they have been verified and published by authorised ministry administrators.</p><Link href="/programmes" className="arrow-link">Back to programmes <Icon name="arrow" size={18} /></Link></div><aside className="notice-card"><strong>{programme.status}</strong><p>This page is intentionally conservative with facts. No programme outcomes, contact details, eligibility claims, or statistics have been invented.</p></aside></section></main>; }
