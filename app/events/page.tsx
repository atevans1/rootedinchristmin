import Link from "next/link";
import { publishedRows } from "@/lib/rooted-content";
export const metadata = { title: "Events", description: "Upcoming Rooted In Christ Ministry events." };
export default async function EventsPage() { const events = await publishedRows("events"); return <main id="main-content" className="simple-page"><section className="page-hero"><div className="container"><p className="eyebrow light">Gather with us</p><h1>There is room at the table.</h1><p>Find ministry gatherings, training, outreach, and community events.</p></div></section><section className="container events-page"><div className="stories-intro"><p className="eyebrow">Events calendar</p><h2>Make space for connection.</h2></div>{events.length ? <div className="event-list">{events.map((event) => <article className="event-card" key={String(event.id)}><p className="card-kicker">{String(event.category || "Ministry event")}</p><h3>{String(event.title)}</h3><p>{String(event.description || "")}</p><time dateTime={String(event.start_at)}>{new Date(String(event.start_at)).toLocaleDateString()}</time></article>)}</div> : <div className="events-empty"><p className="eyebrow">No events published yet</p><h3>Verified gatherings will appear here.</h3><Link href="/contact" className="arrow-link">Ask about upcoming events</Link></div>}</section></main>; }