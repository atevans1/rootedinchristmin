import Link from "next/link";
import { Icon } from "@/components/icon";
import { eventCategories } from "@/lib/events";
export const metadata = { title: "Events", description: "Upcoming and past Rooted In Christ Ministry events." };
export default function EventsPage() { return <main id="main-content" className="simple-page"><section className="page-hero"><div className="container"><p className="eyebrow light">Gather with us</p><h1>There is room at the table.</h1><p>Find verified ministry gatherings, training, outreach, and community events.</p></div></section><section className="container events-page"><div className="stories-intro"><p className="eyebrow">Events calendar</p><h2>Make space for connection.</h2><p>Event details will be published by authorised ministry administrators with confirmed dates, locations, accessibility information, and contact pathways.</p></div><div className="event-filter" aria-label="Event categories">{eventCategories.map((category, index) => <span className={index === 0 ? "active" : ""} key={category}>{category}</span>)}</div><div className="events-empty"><div className="empty-mark"><Icon name="heart" size={30} /></div><p className="eyebrow">No events published yet</p><h3>Verified gatherings will appear here.</h3><p>This calendar is ready for approved ministry events. Nothing is shown until details have been confirmed.</p><Link href="/contact" className="arrow-link">Ask about upcoming events <Icon name="arrow" size={18} /></Link></div></section></main>; }
