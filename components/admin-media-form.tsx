"use client";
import { useActionState } from "react";
import { uploadMedia, type MediaState } from "@/app/admin/content/gallery/actions";
const initial: MediaState = { ok: false, message: "" };
export function AdminMediaForm() { const [state, action, pending] = useActionState(uploadMedia, initial); return <form className="contact-form" action={action} encType="multipart/form-data"><label>Gallery title<input name="title" required maxLength={180} placeholder="Used as the base title for each image" /></label><label>Images<input name="files" type="file" accept="image/jpeg,image/png,image/webp" multiple required /><small>Select several images at once. Each image must be 5 MB or smaller.</small></label><label>Caption<textarea name="caption" rows={4} maxLength={500} /></label><button className="button" disabled={pending} type="submit">{pending ? "Uploading images…" : "Publish images to gallery"}</button>{state.message && <p className={state.ok ? "form-success" : "form-error"} role="status">{state.message}</p>}</form>; }