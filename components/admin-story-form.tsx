"use client";
import { useActionState } from "react";
import { createStory, type StoryState } from "@/app/admin/content/stories/actions";
const initial: StoryState = { ok: false, message: "" };
export function AdminStoryForm() { const [state, action, pending] = useActionState(createStory, initial); return <form className="contact-form" action={action}><label>Story title<input name="title" required maxLength={180} /></label><label>Category<select name="category" required defaultValue=""><option value="" disabled>Select one</option><option>Ministry news</option><option>Outreach report</option><option>Testimony</option><option>Project update</option><option>Announcement</option></select></label><label>Story content<textarea name="content" required minLength={20} maxLength={10000} rows={10} /></label><button className="button" disabled={pending} type="submit">{pending ? "Saving…" : "Publish story"}</button>{state.message && <p className={state.ok ? "form-success" : "form-error"} role="status">{state.message}</p>}</form>; }
