"use client";
import { useActionState } from "react";
import { createEvent, type EventState } from "@/app/admin/content/events/actions";
const initial: EventState = { ok: false, message: "" };
export function AdminEventForm() { const [state, action, pending] = useActionState(createEvent, initial); return <form className="contact-form" action={action}><label>Event title<input name="title" required maxLength={180} /></label><label>Category<select name="category" required defaultValue=""><option value="" disabled>Select one</option><option>Training</option><option>Outreach</option><option>Gathering</option><option>Conference</option></select></label><label>Date<input name="event_date" type="date" required /></label><label>Description<textarea name="description" required minLength={10} maxLength={5000} rows={7} /></label><button className="button" disabled={pending} type="submit">{pending ? "Saving…" : "Save event draft"}</button>{state.message && <p className={state.ok ? "form-success" : "form-error"} role="status">{state.message}</p>}</form>; }
