"use client";
import { useActionState } from "react";
import { submitVolunteer, type VolunteerState } from "@/app/get-involved/volunteer/actions";
const initial: VolunteerState = { ok: false, message: "" };
export function VolunteerForm() { const [state, action, pending] = useActionState(submitVolunteer, initial); return <form className="contact-form" action={action}><label>Full name<input name="full_name" required maxLength={120} autoComplete="name" /></label><label>Email<input name="email" type="email" required maxLength={254} autoComplete="email" /></label><label>Phone<input name="phone" type="tel" autoComplete="tel" /></label><label>Location<input name="location" /></label><label>Profession<input name="profession" /></label><label>Skills<textarea name="skills" required minLength={2} rows={3} placeholder="What would you be glad to contribute?" /></label><label>Areas of interest<textarea name="areas_of_interest" required minLength={2} rows={3} /></label><label>Availability<input name="availability" required placeholder="For example: weekdays, weekends, or occasionally" /></label><label>Preferred programme<input name="preferred_programme" /></label><label>Previous experience<textarea name="previous_experience" rows={3} /></label><label>Additional notes<textarea name="additional_notes" rows={3} /></label><button className="button" type="submit" disabled={pending}>{pending ? "Submitting…" : "Submit application"}</button>{state.message && <p className={state.ok ? "form-success" : "form-error"} role="status">{state.message}</p>}</form>; }
