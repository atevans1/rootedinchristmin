"use client";
import { useActionState } from "react";
import { submitPartner, type PartnerState } from "@/app/get-involved/partner/actions";
const initial: PartnerState = { ok: false, message: "" };
export function PartnerForm() { const [state, action, pending] = useActionState(submitPartner, initial); return <form className="contact-form" action={action}><label>Organisation or person name<input name="organisation_name" required maxLength={160} /></label><label>Contact name<input name="contact_name" required maxLength={120} autoComplete="name" /></label><label>Email<input name="email" required type="email" maxLength={254} autoComplete="email" /></label><label>Phone<input name="phone" type="tel" autoComplete="tel" /></label><label>Partnership type<select name="partnership_type" required defaultValue=""><option value="" disabled>Select one</option><option>Individual</option><option>Church or ministry</option><option>Company or corporate organisation</option><option>NGO or foundation</option><option>Institution</option></select></label><label>Area of interest<input name="area_of_interest" required /></label><label>Message<textarea name="message" required minLength={10} maxLength={5000} rows={6} /></label><button className="button" type="submit" disabled={pending}>{pending ? "Sending…" : "Send enquiry"}</button>{state.message && <p className={state.ok ? "form-success" : "form-error"} role="status">{state.message}</p>}</form>; }
